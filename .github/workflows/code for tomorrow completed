#include <iostream>
using namespace std;

int main(){
	int rows,cols;
	
	cout << "Enter the number or rows: ";
	cin >> rows;
	cout << "Enter the number of columns; ";
	cin >> cols;
	
	char leftBracket = char(91);
	char rightBracket = char(93);
	
	for (int i =1; i <= rows;i++) {
		for (int j = 1; j<= cols;j++){
			if (j==1 ||i==rows||j==1||)
		}
	}
}
